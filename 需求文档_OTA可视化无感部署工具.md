# OTA 可视化无感部署工具 — 需求文档

> 版本：v1.0  
> 日期：2026-06-08  
> 定位：Windows 端 PyQt 可视化无感部署工具

---

## 一、工具定位

Windows 端可视化无感部署工具，替代现有 `ota_upgrade_replace.sh` 脚本的 CLI 操作方式，提供图形化界面，支持多包选择、按流程自动传输替换，适配不同包的操作差异，兼顾稳定性与用户体验。

---

## 二、核心流程总览

```
用户选择文件 → 配置加载 → 文件传输（Win → 工控机 → 板端）
    → 传输前处理（清理/空间检查/备份）
    → 板端操作（解压/赋权/流程映射）
    → 多包顺序执行 + 重启监听
    → 失败时自动回滚
    → 日志记录
```

---

## 三、功能需求详情

### 3.1 界面交互（PyQt）

| 组件 | 说明 |
|------|------|
| **本地文件选择区** | 支持单选/多选 `.zip` 升级包，显示已选文件列表 |
| **"执行板端数据清理" 复选框** | 勾选后启用清理路径输入框 |
| **清理路径输入框** | 可输入板端待清理目录路径（如 `/data/log`），支持多路径 |
| **"开始部署" 按钮** | 触发整个部署流程 |
| **"查看日志" 按钮** | 打开集成日志查看器 |
| **进度条显示区** | 实时显示当前传输/操作进度百分比 |
| **"保存配置" 按钮** | 将当前界面配置保存到加密配置文件 |
| **包类型选择下拉框（可选）** | 预留扩展，后续可手动指定包类型覆盖自动识别 |

### 3.2 配置与权限预处理

#### 3.2.1 配置文件内容（加密存储）

继承自 `ota_upgrade_replace.sh` 的配置项，并扩展：

| 配置项 | 说明 | 来源 |
|--------|------|------|
| `工控机_IP` | 中间跳板机 IP | 新增 |
| `工控机_用户名` | 工控机 SSH 用户名 | 新增 |
| `工控机_密码` | 工控机 SSH 密码 | 新增 |
| `板端_IP` | 目标板端 IP | 继承脚本 `TARGET_IP` |
| `板端_用户名` | 板端 SSH 用户名（默认 `root`） | 继承 `TARGET_USER` |
| `板端_密码` | 板端 SSH 密码 | 新增 |
| `板端_OTA目录` | 板端 OTA 临时目录（默认 `/ota`） | 继承 `REMOTE_OTA_DIR` |
| `板端_APP目录` | 板端 APP 安装目录（默认 `/app`） | 继承 `REMOTE_APP_DIR` |
| `SSH_超时` | SSH 连接超时秒数（默认 8） | 继承 `SSH_TIMEOUT` |
| `重启_离线等待` | 等待设备离线超时（默认 60s） | 继承 `REBOOT_OFFLINE_WAIT` |
| `重启_在线等待` | 等待设备上线超时（默认 300s） | 继承 `REBOOT_ONLINE_WAIT` |
| `额外预留空间_KB` | 额外预留空间（默认 102400KB） | 继承 `MIN_EXTRA_KB` |
| `本机IP_目标IP_映射表` | 本机 IP → 板端 IP 映射关系 | 继承 `resolve_target_ip_by_vehicle_ip` |

#### 3.2.2 IP 映射表（继承自脚本）

| 本机（工控机）IP | 板端目标 IP |
|-----------------|------------|
| 172.31.48.102 | 172.31.48.9 |
| 192.168.2.102 | 192.168.2.62 |
| 172.20.1.102 | 172.20.1.22 |
| 192.168.62.102 | 192.168.62.62 |

- 工具启动时自动检测工控机本机 IP，根据映射表自动填充板端 IP
- 用户可手动修改板端 IP，覆盖自动映射
- 映射表可在配置文件中编辑扩展

#### 3.2.3 包类型分类规则（继承自脚本）

| 文件名关键字 | 包类型 | 目标目录 | 说明 |
|-------------|--------|---------|------|
| `mcu` | `mcu` | — | MCU 固件，使用 `ota_tool` 升级 |
| `drivememomap` | `map` | — | 地图包，解压后执行 `map_update.sh9`/`map_update.sh` |
| `pvs` / `vff` / `g6` | `full` | — | 整包升级，使用 `ota_tool` |
| `env_model` | `app` | `env_model` | APP 替换升级 |
| `odometry` | `app` | `odometry` | APP 替换升级 |
| `encryption` | `app` | `encryption` | APP 替换升级 |
| `trigger` | `app` | `trigger` | APP 替换升级 |
| `fsd_control` | `app` | `fsd_control` | APP 替换升级 |
| `guard` | `app` | `guard` | APP 替换升级 |
| `dsp` | `app` | `dsp` | APP 替换升级 |
| `ipcf_proxy` | `app` | `ipcf_proxy` | APP 替换升级 |
| `para_check` | `app` | `para_check` | APP 替换升级 |
| `planning_node` | `app` | `planning_node` | APP 替换升级 |
| `system_monitor` | `app` | `system_monitor` | APP 替换升级 |
| `dpsm` | `app` | `dpsm` | APP 替换升级 |
| `perception` | `app` | `adas` | 特殊映射 |
| `calibration` | `app` | `calib_app` | 特殊映射 |

- 分类规则支持通过配置文件手动编辑扩展

### 3.3 文件传输（Win → 工控机 → 板端）

#### 3.3.1 传输链路

```
[Windows 本机] --(paramiko SFTP)--> [工控机] --(paramiko SSH 调用 scp)--> [板端 /ota 目录]
```

#### 3.3.2 传输流程

1. **Step 1**：通过 `paramiko` SFTP 将用户选择的 `.zip` 包从 Windows 上传到工控机临时目录
2. **Step 2**：通过 `paramiko` SSH 在工控机上执行 `scp` 命令，将文件从工控机传输到板端 `/ota` 目录
3. **进度反馈**：SFTP 传输时通过 `paramiko` 的 `callback` 函数实时更新界面进度条；SCP 阶段通过解析 `scp` 输出或预估大小更新进度

#### 3.3.3 传输前检查（继承脚本逻辑）

- **空间检查**：通过 `df -Pk /ota` 检查板端 `/ota` 可用空间，需满足 `包大小 + 额外预留 100MB`
- **空间不足处理**：
  - 弹窗提示用户空间不足
  - 提供"自动清理 `/ota` 目录"选项
  - 清理后重新检查，仍不足则中断并报错
- **备份**：对板端 `/app/<target_dir>` 原目录创建带时间戳的备份到 `/ota/<target_dir>_old_<timestamp>`

### 3.4 板端操作

#### 3.4.1 通用操作

- **挂载读写**：`mount -o remount rw /app`
- **赋权**：`chmod -R 755 <目标目录>`，通过读取命令返回值确认权限生效
- **清理**：若用户勾选"执行板端数据清理"，在传输前执行预设清理命令（如 `rm -rf /data/log/*`）

#### 3.4.2 包类型-操作流程映射表

| 包类型 | 操作流程 | 继承自脚本函数 |
|--------|---------|---------------|
| **app** | ① 备份原目录 → ② `unzip` 解压到 `/app` → ③ 删除上传包 → ④ `sync` → ⑤ `reboot` → ⑥ 监听 SSH 断开 → ⑦ 循环 ping 确认重启上线 | `upgrade_app_package` |
| **mcu** | ① `ota_tool -p <pkg>:mcu` → ② 不执行 sync/reboot（由 ota_tool 自行管理） | `upgrade_mcu_package` |
| **full** | ① `ota_tool -p <pkg>` → ② SSH 断开（255）视为正常 → ③ 监听重启上线 | `upgrade_full_package` |
| **map** | ① `unzip` 解压 → ② 查找并执行 `map_update.sh9`/`map_update.sh` → ③ `sync` → ④ `reboot` → ⑤ 监听重启上线 | `upgrade_map_package` |

#### 3.4.3 流程监听机制

- **SSH 退出状态监听**：执行远程命令后，监听 SSH 会话退出码
  - `full` 类型：退出码 255（连接中断）视为正常，因板端自动重启
  - 其他异常退出码：视为失败，触发回滚
- **重启确认**：通过循环 `ping` 或 SSH 连接测试确认板端重启完成
  - 离线等待：最多 60s（`REBOOT_OFFLINE_WAIT`）
  - 在线等待：最多 300s（`REBOOT_ONLINE_WAIT`）
  - 超时则中断并触发回滚

#### 3.4.4 多包顺序执行

- 多包处理时，按用户选择顺序（或预设优先级：`full → map → mcu → app`）依次执行
- 前一个包流程完全完成（含重启确认连通）后再启动下一个
- 若某一步失败，自动执行回滚操作

### 3.5 回滚机制

| 触发条件 | 回滚操作 |
|---------|---------|
| 板端操作命令失败 | 将临时备份目录移回原位置：`mv /ota/<backup> /app/<target_dir>` |
| 重启等待超时 | 同上，并弹窗提示 |
| 板端输出匹配错误关键词 | 立即中断当前流程，执行回滚，弹窗提示具体错误 |

- 回滚后记录日志，标记该包升级失败
- 后续包是否继续执行由用户确认（弹窗询问）

### 3.6 日志与异常处理

#### 3.6.1 日志记录

- **日志文件**：所有后台命令输出、板端日志实时写入本地日志文件（按日期命名，如 `deploy_20260608.log`）
- **日志内容**：
  - 时间戳 + 日志级别（INFO/WARN/ERROR）
  - 执行的命令及输出
  - 板端返回的原始输出
  - 错误堆栈信息
  - 操作耗时

#### 3.6.2 日志查看器

- 通过界面"查看日志"按钮打开集成日志查看器
- 功能：
  - **时间筛选**：按日期/时间范围过滤
  - **关键词搜索**：支持输入关键词高亮/过滤
  - **日志级别过滤**：按 INFO/WARN/ERROR 筛选
  - **自动滚动**：实时监控时自动滚动到最新日志

#### 3.6.3 异常监控

- 实时监控板端 SSH 输出，匹配以下错误关键词时立即中断：
  - `空间不足` / `No space left`
  - `权限拒绝` / `Permission denied`
  - `未找到` / `No such file` / `not found`
  - `命令未找到` / `command not found`
- 匹配到错误关键词时：
  1. 立即中断当前流程
  2. 执行回滚操作
  3. 弹窗提示具体错误信息
  4. 记录错误日志

### 3.7 后续可编辑空间

| 可编辑项 | 编辑方式 | 说明 |
|---------|---------|------|
| **界面布局** | 代码层面调整 | 按钮位置、新增包类型选择下拉框等 |
| **映射表配置文件** | 手动编辑配置文件 | 添加新包流程、修改命令参数、新增 IP 映射 |
| **清理/赋权命令** | 工具设置界面 | 在工具设置界面自定义清理命令和赋权命令内容 |
| **包类型分类规则** | 编辑配置文件 | 新增文件名关键字→包类型映射 |
| **SSH/超时参数** | 配置界面 | 修改超时时间、重试次数等 |

---

## 四、技术选型建议

| 技术 | 用途 |
|------|------|
| **Python 3.8+** | 开发语言 |
| **PyQt5 / PySide6** | GUI 框架 |
| **paramiko** | SSH/SFTP 客户端（替代 shell 脚本中的 ssh/scp） |
| **cryptography** | 配置文件加密（如 Fernet 对称加密） |
| **logging** | Python 标准日志模块 |
| **pyinstaller** | 打包为 Windows 可执行文件（.exe） |

---

## 五、与现有脚本的对应关系

| 现有脚本函数/变量 | 需求中的对应 |
|------------------|-------------|
| `resolve_target_ip_by_vehicle_ip()` | 配置模块中的 IP 映射表自动填充 |
| `classify_package()` | 包类型分类规则表 |
| `check_remote_ota_space()` | 传输前空间检查 |
| `upgrade_app_package()` | app 类型操作流程 |
| `upgrade_mcu_package()` | mcu 类型操作流程 |
| `upgrade_full_package()` | full 类型操作流程 |
| `upgrade_map_package()` | map 类型操作流程 |
| `wait_for_device_online_between_packages()` | 重启监听 + 多包等待机制 |
| `backup` 逻辑（`mv ..._old_<ts>`） | 备份与回滚机制 |
| `SSH_OPTS` / `SSH_TIMEOUT` | 配置项中的 SSH 参数 |
| `trap ERR` / `on_error()` | 异常监控与错误关键词匹配 |
| `confirm()` 交互 | GUI 弹窗确认 |

---

## 六、非功能需求

1. **稳定性**：传输中断可重试，SSH 连接自动重连（最多 3 次）
2. **安全性**：配置文件加密存储，密码不明文显示
3. **可用性**：操作步骤有明确提示，进度可视化，错误信息清晰
4. **可扩展性**：包类型映射、操作流程、IP 映射均可通过配置文件扩展
5. **兼容性**：支持 Windows 10/11，打包为独立 exe，无需安装 Python 环境
